# skills/_artifacts/skill_tree.yaml
# Generated by skill-tree-generator (Workflow A — Generate)
library:
  name: '@michaelrwalker/stagehand'
  version: '0.5.3'
  repository: 'https://github.com/miaklwalker/stagehand'
  description: 'A fully typesafe, saga-style script framework for TypeScript with a live terminal UI.'
generated_from:
  domain_map: 'skills/_artifacts/domain_map.yaml'
  skill_spec: 'skills/_artifacts/skill_spec.md'
generated_at: '2026-08-19'

# Structure decision: FLAT, grouped by domain folder (skills/<domain>/<skill>/SKILL.md).
# Rationale: single package, no framework adapters, no monorepo — the "core vs framework
# split" that motivates the nested [lib]-core/[domain] + react-[lib] layout doesn't apply
# here. All 12 skills are independently-loadable task moments (per skill-domain-discovery's
# task-focused design), which is squarely the "flat" case per skill-tree-generator's own
# guidance ("Skills map 1:1 to distinct developer intents with minimal overlap"). Domain
# folders are kept purely for filesystem organization, not as a core-overview/registry
# layer — there is no [lib]-core SKILL.md and no sub-skill `requires` chain to it.
# No router skill: 12 skills is below the 15+ threshold where browsing `intent list`
# stops being sufficient, and the CLI's list/show commands already provide discovery.

skills:
  - name: 'Getting started with a first script'
    slug: 'getting-started'
    type: 'lifecycle'
    domain: 'building-scripts'
    path: 'skills/building-scripts/getting-started/SKILL.md'
    description: 'Build a complete Stagehand script end to end — Script constructor, addPhase/addStep, a handler that widens the typed context, a rollback, and reading the RunResult. Load this first for any "write my first script" or "turn this async function chain into Stagehand" request.'
    sources:
      - 'miaklwalker/stagehand:README.md'
      - 'miaklwalker/stagehand:docs/quick-start.md'
      - 'miaklwalker/stagehand:docs/guides/script-options-and-result.md'
      - 'miaklwalker/stagehand:docs/guides/cleaning-context.md'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:src/types.ts'

  - name: 'Structuring phases and steps'
    slug: 'structuring-phases-and-steps'
    type: 'core'
    domain: 'building-scripts'
    path: 'skills/building-scripts/structuring-phases-and-steps/SKILL.md'
    description: 'addPhase/addStep mechanics, phase and step naming rules, the implicit "Main" phase, per-step retry and timeoutMs, and outline(). Load this for "group these steps into phases," "retry a flaky step," "add a timeout," or "inspect a script''s structure without running it."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/phases-and-steps.md'
      - 'miaklwalker/stagehand:docs/guides/script-options-and-result.md'
      - 'miaklwalker/stagehand:docs/guides/reusable-scripts.md'
      - 'miaklwalker/stagehand:docs/reference/errors.md'
      - 'miaklwalker/stagehand:docs/reference/script.md'
      - 'miaklwalker/stagehand:src/script.ts'

  - name: 'The typed context and validated input'
    slug: 'typed-context-and-input'
    type: 'core'
    domain: 'building-scripts'
    path: 'skills/building-scripts/typed-context-and-input/SKILL.md'
    description: 'How ctx accumulates purely from handler return types with zero hand-written generics, the input-vs-ctx distinction, and defineInput for validating run() input with any Standard Schema library (Zod, Valibot, ArkType, Effect Schema). Load this for "validate my script''s input" or "why is this key missing from ctx."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/typed-context.md'
      - 'miaklwalker/stagehand:docs/reference/errors.md'
      - 'miaklwalker/stagehand:docs/reference/script.md'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:src/types.ts'
    references:
      - 'references/context-type-utilities.md'

  - name: 'Adding rollbacks'
    slug: 'adding-rollbacks'
    type: 'core'
    domain: 'compensation-safety'
    path: 'skills/compensation-safety/adding-rollbacks/SKILL.md'
    description: 'Writing a rollback, declaring rollbackKeys (what it narrows and permanently reserves), ScriptOptions.rollback scope (all/phase/none), what a rollback can and cannot see, and cancellation via AbortSignal/Ctrl-C. Load this for "add a compensation to a mutation step," "why can''t my rollback see this key," or "cancel a running script."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/rollbacks.md'
      - 'miaklwalker/stagehand:docs/reference/errors.md'
      - 'miaklwalker/stagehand:src/errors.ts'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:test/rollback-keys.test.ts'

  - name: 'Cleaning context keys'
    slug: 'cleaning-context'
    type: 'core'
    domain: 'compensation-safety'
    path: 'skills/compensation-safety/cleaning-context/SKILL.md'
    description: 'The clean field: dropping keys from both the runtime context and the type every later step sees, and its hard interaction with rollbackKeys reservation. Load this for "drop a secret from context once it''s no longer needed" or "why does clean reject this key."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/cleaning-context.md'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:test/rollback-keys.test.ts'
      - 'miaklwalker/stagehand:test/clean.test.ts'

  - name: 'Conditional steps and phases'
    slug: 'conditional-steps'
    type: 'core'
    domain: 'compensation-safety'
    path: 'skills/compensation-safety/conditional-steps/SKILL.md'
    description: 'when on a step or a whole phase, skip semantics, and how a skip interacts with rollback and with a mounted routine''s own input. Load this for "run this step only in production" or "skip a whole phase based on input."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/phases-and-steps.md'
      - 'miaklwalker/stagehand:docs/guides/reusable-scripts.md'
      - 'miaklwalker/stagehand:src/script.ts'

  - name: 'Reporting progress from a step'
    slug: 'reporting-progress'
    type: 'core'
    domain: 'observability'
    path: 'skills/observability/reporting-progress/SKILL.md'
    description: 'The handler UI surface: status(text) vs note(text), progress({total, label}), task(label)/tasks([...] as const), and log/info/warn/error/success. Load this for "show a progress bar," "put a row count on the finished step line," or "run a typed checklist inside a step."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/handler-surface.md'
      - 'miaklwalker/stagehand:src/context.ts'
      - 'miaklwalker/stagehand:src/script.ts'

  - name: 'Log placement and terminal rendering'
    slug: 'log-placement-and-rendering'
    type: 'core'
    domain: 'observability'
    path: 'skills/observability/log-placement-and-rendering/SKILL.md'
    description: 'logPlacement modes (scrollback/step/bottom) and how the live TTY frame differs from the plain/CI renderer, including which behaviors exist only in one of the two. Load this for "nest a rollback''s log under the step it''s undoing," "force plain output for CI," or "match my own console output to the script''s styling."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/handler-surface.md'
      - 'miaklwalker/stagehand:docs/guides/terminal-rendering.md'
      - 'miaklwalker/stagehand:test/log-placement.test.ts'
      - 'miaklwalker/stagehand:test/note.test.ts'

  - name: 'Caching expensive work'
    slug: 'caching-expensive-work'
    type: 'core'
    domain: 'caching'
    path: 'skills/caching/caching-expensive-work/SKILL.md'
    description: 'fileStore/memoryStore, stale/schema, per-run CacheMode (on/off/refresh/read-only), and reaching a cached slot with context.cache (read/write/clear/ageOf) from a later step or rollback. Load this for "skip re-running an expensive phase," "invalidate a cache entry from a later step," or "guard against an old script shape."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/caching.md'
      - 'miaklwalker/stagehand:docs/reference/cache.md'
      - 'miaklwalker/stagehand:src/cache.ts'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:test/cache.test.ts'
      - 'miaklwalker/stagehand:test/context-cache.test.ts'
    references:
      - 'references/cache-api.md'

  - name: 'Splitting steps across files'
    slug: 'splitting-steps-across-files'
    type: 'core'
    domain: 'reuse'
    path: 'skills/reuse/splitting-steps-across-files/SKILL.md'
    description: 'stepFor binds the In/Ctx a step declared outside any script expects; WithStepFor composes a chain''s context without restating each step''s output by hand. Covers building a shared library of small, composable steps with simple inputs for a recurring workflow. Load this for "move a step into its own module" or "build a reusable steps file for [workflow]."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/splitting-steps.md'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:test/with-step-for.test.ts'

  - name: 'Reusable routines and mounts'
    slug: 'reusable-routines-and-mounts'
    type: 'core'
    domain: 'reuse'
    path: 'skills/reuse/reusable-routines-and-mounts/SKILL.md'
    description: 'routineFor declares a mountable group of phases; use() splices it (or a plain Script) into a host, with optional as-renaming and per-mount input mapping. Load this for "share a fetch-and-normalize pipeline across scripts" or "mount the same routine twice for two tenants."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/reusable-scripts.md'
      - 'miaklwalker/stagehand:docs/guides/caching.md'
      - 'miaklwalker/stagehand:docs/reference/errors.md'
      - 'miaklwalker/stagehand:docs/reference/script.md'
      - 'miaklwalker/stagehand:src/script.ts'
      - 'miaklwalker/stagehand:test/routine.test.ts'

  - name: 'Handling results and errors'
    slug: 'handling-results-and-errors'
    type: 'core'
    domain: 'errors-results'
    path: 'skills/errors-results/handling-results-and-errors/SKILL.md'
    description: 'The RunResult discriminated union, every exported error class (StepFailedError, StepTimeoutError, ScriptAbortedError, RollbackFailedError, CacheShapeError, SchemaValidationError), isAbort, and throwOnError. Load this for "branch correctly on a failed run" or "tell a Ctrl-C cancellation apart from a real failure."'
    sources:
      - 'miaklwalker/stagehand:docs/guides/rollbacks.md'
      - 'miaklwalker/stagehand:docs/guides/script-options-and-result.md'
      - 'miaklwalker/stagehand:docs/reference/errors.md'
      - 'miaklwalker/stagehand:src/errors.ts'
      - 'miaklwalker/stagehand:src/script.ts'
